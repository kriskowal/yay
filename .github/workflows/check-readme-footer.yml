name: Check README Footer

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check-footer:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check README footer sections
        run: ./scripts/test-readme-footer.sh
